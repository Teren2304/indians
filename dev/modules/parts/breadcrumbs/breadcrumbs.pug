mixin breadcrumbs(value)
	.breadcrumbs.flex-container.align-middle&attributes(attributes)
		each item in value
			if (item.link)
				a.font.breadcrumbs__item(href=item.link)= item.name
			else
				p.font.breadcrumbs__item= item.name